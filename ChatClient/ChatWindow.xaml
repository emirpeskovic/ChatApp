<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatClient"
        xmlns:Control="clr-namespace:ChatClient.Control" x:Class="ChatClient.ChatWindow"
        mc:Ignorable="d"
        Width="674"
        Height="auto"
        SizeToContent="WidthAndHeight"
        Title="ChatWindow">
    <Grid MinWidth="400" MinHeight="400" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="192*"/>
            <RowDefinition Height="25*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="179*"/>
            <ColumnDefinition Width="621*"/>
        </Grid.ColumnDefinitions>

        <Control:ScrollableChatUserControl x:Name="chatUserControl" Grid.Column="1"/>
        <Button Content="Button"/>
    </Grid>
</Window>
